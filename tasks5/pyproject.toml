[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "task-cli"
version = "0.1.0"
description = "Simple CLI task manager storing tasks in a JSON file"
authors = [{name = "spec-generator"}]
requires-python = ">=3.11"
